---
name: "tdd"
description: "Toggle test-first workflow mode on or off."
argument-hint: "on|off"
agent: "agent"
---

Use @.ai/commands/tdd.md as the canonical command definition.
Follow it exactly.
Treat the text supplied by the user when invoking this prompt as the command input.
