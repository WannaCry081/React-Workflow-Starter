---
name: "commit"
description: "Analyze active git changes and create atomic Conventional Commits in sequence."
argument-hint: "Optional: baseRef=<ref> maxCommits=<n> includeUntracked=<true|false>"
agent: "agent"
---

Use @.ai/commands/commit.md as the canonical command definition.
Follow it exactly.
Treat the text supplied by the user when invoking this prompt as the command input.
