---
name: "ask"
description: "Refine a request into a minimal, token-efficient prompt before planning or coding."
argument-hint: "Paste the request to refine"
agent: "agent"
---

Use @.ai/commands/ask.md as the canonical command definition.
Follow it exactly.
Treat the text supplied by the user when invoking this prompt as the command input.
